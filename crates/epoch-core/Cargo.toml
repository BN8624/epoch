[package]
name = "epoch-core"
version = "0.1.0"
edition.workspace = true
description = "EPOCH deterministic core — command schedule, events, and replay"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
