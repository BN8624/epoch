[package]
name = "epoch-app"
version = "0.1.0"
edition.workspace = true
description = "EPOCH generated-world observer — RightsWorld export and static UI"

[[bin]]
name = "epoch-app"
path = "src/main.rs"

[dependencies]
epoch-core = { path = "../epoch-core" }
serde_json = { workspace = true }
